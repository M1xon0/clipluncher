{
  "name": "clipluncher",
  "version": "1.0.0",
  "description": "ClipSMP Minecraft Launcher - Based on x-minecraft-launcher",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
